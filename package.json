{
  "name": "reorg-pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Turn one freeform reorg message into a ChangeSet (Slice A), then walk a declared graph of stub writes and human gates (Slice B). Never calls a model in Slice B.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/cli.js --all",
    "cli": "node src/cli.js",
    "reset": "rm -rf out",
    "test": "node scripts/test-redact.js && node scripts/test-slice-b.js && node src/cli.js --all"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "dotenv": "^17.4.2"
  }
}
