{
  "name": "reorg-pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Slice A: turn one freeform reorg message into a structured ChangeSet, or abstain. Never touches a target system.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/cli.js --all",
    "cli": "node src/cli.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "dotenv": "^17.4.2"
  }
}
